@PART[*]:HAS[@MODULE[MultiModeEngine],@MODULE[ModuleEngines*]:HAS[@PROPELLANT[IntakeAir]],!MODULE[ModuleAutoSwitchJetEngine]]:FINAL
{
    MODULE
    {
        name = ModuleAutoSwitchJetEngine
        // Note: threshold and enabled state are now handled by external file (AutoSwitchMaster),
        // but these keys can remain here as fallbacks or placeholders if needed, 
        // though the C# code prioritizes the Profile system.
    }
}